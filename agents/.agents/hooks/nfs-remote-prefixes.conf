# Extra shared-directory prefixes for block-nfs-writes (one absolute path per
# line; '#' starts a comment). These are matched on remote experiment hosts
# (inside ssh commands, scp/rsync host:/path destinations) AND locally.
# Built-in defaults (always active, no need to list): /proj /share /work
