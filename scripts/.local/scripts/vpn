#!/bin/bash
set -euo pipefail
# VT VPN via Tailscale exit node (popos lab machine)

EXIT_NODE="popos"
OS="$(uname -s)"

# Detect tailscale binary (macOS app vs standard CLI)
if [[ "$OS" == "Darwin" ]] && [[ -f "/Applications/Tailscale.app/Contents/MacOS/Tailscale" ]]; then
    TS_CMD="/Applications/Tailscale.app/Contents/MacOS/Tailscale"
else
    TS_CMD="tailscale"
fi

# Check if currently using an exit node
current=$("$TS_CMD" status --json | jq -r '.ExitNodeStatus.ID // empty')

if [[ -n "$current" ]]; then
    # Turn VPN off
    if [[ "$OS" == "Linux" ]]; then
        sudo tailscale set --exit-node=
    else
        "$TS_CMD" set --exit-node=
    fi
    echo "VPN: off"
else
    # Turn VPN on. No manual /etc/resolv.conf surgery: with accept-dns on,
    # tailscaled already routes DNS through the exit node itself, and
    # hand-writing 100.100.100.100 poisons tailscaled's snapshot of the
    # system DNS config (self-loop, see tailscale.com/s/resolvconf-overwrite).
    if [[ "$OS" == "Linux" ]]; then
        sudo tailscale set --exit-node="$EXIT_NODE"
    else
        "$TS_CMD" set --exit-node="$EXIT_NODE"
    fi
    echo "VPN: on"
fi
